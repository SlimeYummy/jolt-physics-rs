[package]
name = "jolt-physics-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
cxx = { version = "1.0", features = ["default", "c++17"] }
glam = { version = "0.25", features = ["approx", "serde"] }
libc = "0.2"
serde = { version = "1.0", features = ["derive", "rc"] }
static_assertions = "1.1"

[build-dependencies]
cxx-build = "1.0"
vcvars = "0.1.6"

[features]
default = ["deterministic"]
deterministic = []
profile = []
debug-renderer = []

[[bin]]
name = "testbed"
required-features = ["debug-renderer"]
